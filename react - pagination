```
my-project/
├─ index.html
├─ src/
│  ├─ Pagination.jsx
│  ├─ main.jsx
│  ├─ App.css
│  ├─ index.css
```

---

### 🧩 `src/Pagination.jsx`

```jsx
import React, { useState } from 'react'
import './App.css'

function Pagination() {
  // create mock data of 50 items
  const data = Array.from({ length: 50 }, (_, i) => ({
    id: i + 1,
    name: `Item ${i + 1}`,
  }))

  const itemsPerPage = 5
  const [page, setPage] = useState(1)

  const totalPages = Math.ceil(data.length / itemsPerPage)
  const start = (page - 1) * itemsPerPage
  const paginatedData = data.slice(start, start + itemsPerPage)

  return (
    <div className="container">
      <h2>Pagination with Table</h2>

      <table className="table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Item Name</th>
          </tr>
        </thead>
        <tbody>
          {paginatedData.map((row) => (
            <tr key={row.id}>
              <td>{row.id}</td>
              <td>{row.name}</td>
            </tr>
          ))}
        </tbody>
      </table>

      <div className="buttons">
        <button onClick={() => setPage(Math.max(page - 1, 1))} disabled={page === 1}>
          Prev
        </button>

        <span className="page-info">
          Page {page} of {totalPages}
        </span>

        <button
          onClick={() => setPage(Math.min(page + 1, totalPages))}
          disabled={page === totalPages}
        >
          Next
        </button>
      </div>
    </div>
  )
}

export default Pagination
```

---

### 🧠 `src/main.jsx`

```jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import Pagination from './Pagination.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Pagination />
  </React.StrictMode>,
)
```

---

### 🎨 `src/App.css`

```css
.container {
  text-align: center;
  margin-top: 50px;
  font-family: Arial, sans-serif;
}

h2 {
  margin-bottom: 20px;
}

.table {
  border-collapse: collapse;
  width: 60%;
  margin: 0 auto 20px auto;
}

.table th,
.table td {
  border: 1px solid #ccc;
  padding: 8px;
}

.table th {
  background-color: #f2f2f2;
}

.buttons {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 10px;
}

button {
  padding: 6px 12px;
  font-size: 1rem;
  cursor: pointer;
}

button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.page-info {
  font-weight: bold;
}
```

---

### 🧾 `src/index.css`

```css
/* Simple reset for clean layout */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  background-color: #fff;
  color: #000;
}
```

---

### 🏠 `index.html`

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pagination Table</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

---

### 🚀 Run it

In your project directory:

```bash
npm run dev
```

Then open the local URL (usually `http://localhost:5173/`).
You’ll see a neat **table showing 5 rows per page** with “Prev / Next” navigation.
